apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "theiaide-raimundo.fullname" . }}-test-connection"
  labels:
    {{- include "theiaide-raimundo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "theiaide-raimundo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
