apiVersion: v1
kind: Service
metadata:
  name: {{ include "theiaide-raimundo.fullname" . }}
  labels:
    {{- include "theiaide-raimundo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "theiaide-raimundo.selectorLabels" . | nindent 4 }}
